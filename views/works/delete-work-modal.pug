.modal.delete-work-modal 
    .modal-content
        .mb-4
            h3 Delete work 
            p.italic.gray This action cannot be undone.

        form(action=`/projects/${work.projectId}/works/${work.id}?_method=delete`, method="post") 
                p.mb-4 Are you sure you want to delete this work?

                .text-end.pt-4
                    button.delete-work-close.is-muted.mr-4(type="button") Cancel 
                    button.is-danger(type="submit") Yes